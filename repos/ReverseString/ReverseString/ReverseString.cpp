// ReverseString.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

template <class T>
T split(T split)
{
	return split;
}
void split(vector<string> holder)
{
	//cout << holder;
	//while (holder < holder.size())
	//{

	//}

}
int main()
{
	

	string expression;
	vector<string> s(const string & expression);
	vector<string> vect;
	vector<string> holder;
	int count;
	// Define an iterator object. 
	vector<int>::iterator iter;// Loop counter  
	//char delim;
	
	cout << "Enter an expression: ";
	getline(cin, expression);
	while (expression != "\n")
	{
		if (expression == "0")
		{

		}
	}
	vect.push_back(expression);
	split(vect);

	
		
	
	// Define a vector object. 
	
	
	// Use push_back to push values into the vector. 
		for (count = 0; count < 10; count++)
		{
		}
	// Step the iterator through the vector, 
	// and use it to display the vector's contents. 
	//cout << "Here are the values in vect: ";
	//for (iter = vect.begin(); iter < vect.end(); iter++)
	//{
	//	cout << *iter << " ";
	//}

	//reverse(vect.begin(), vect.end());

	//cout << "myvector contains:";
	//for (iter = vect.begin(); iter != vect.end(); ++iter)
	//	cout << ' ' << *iter;
	//cout << '\n';
}